
<div class="card col-lg-6 mb-4 mx-auto">
    <h2 class="card-header h3 my-2"><% if (currentCategory) { %>Modification<% } else { %>Ajout<% } %> de catégorie</h2>
    <div class="card-body">
        <form action="/categories<% if (currentSlug) { %>/<%= currentSlug %><% } %>" method="post" data-bitwarden-watching="1">
            <fieldset>
                <div class="form-group row">
                    <label for="categoryName" class="col-lg-2 col-form-label">Nom</label>
                    <div class="col-lg-6">
                        <input type="text" class="form-control" name="name" id="categoryName" <% if (currentCategory) { %>value=<%= currentCategory.name %> <% } %>placeholder="Nom de la catégorie" required>
                    </div>
                    <label for="categorySort" class="col-lg-2 col-form-label">Ordre</label>
                    <div class="col-lg-2">
                        <input type="number" class="form-control" name="sort" id="categorySort" value=<% if (currentCategory) { %><%= currentCategory.sort %><% } else { %>0<% } %>>
                    </div>
                </div>
                <div class="form-check form-switch mt-2">
                    <input class="form-check-input" type="checkbox" name="isTrashed" id="flexSwitchCheckChecked"<% if (currentCategory && currentCategory.isTrashed) { %> checked=""<% } %>>
                    <label class="form-check-label" for="flexSwitchCheckChecked">A la corbeille</label>
                </div>
            </fieldset>
            <div class="d-flex justify-content-around mt-4">
                <button type="submit" class="btn btn-sm btn-info"><i class="fa-solid fa-floppy-disk"></i> <% if (currentCategory) { %>Modifier<% } else { %>Ajouter<% } %></button>
                <% if (currentCategory) { %>
                <a href="/categories" class="btn btn-sm btn-outline-primary"><i class="fa-solid fa-rotate-left"></i> Annuler</a>
                <a href="/categories/<%= currentCategory.slug %>/delete" class="btn btn-sm btn-danger"><i class="fa-solid fa-trash"></i> Supprimer</a>
                <% } %>
            </div>
        </form>
    </div>
</div>
