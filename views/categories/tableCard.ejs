<div class="card">
    <h2 class="card-header h3 my-2">Catégories existantes</h2>
    <div class="card-body">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col">Nom</th>
                    <th scope="col">Ordre</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                <% for (category of categories) { %>
                <tr class="<% if (currentSlug === category.slug) { %>table-info<% } else if (category.isTrashed) { %>table-dark text-muted<% } %>">
                    <th scope="row"><%= category.name %></th>
                    <td><%= category.sort %></td>
                    <td class="text-center">
                        <% if (!currentCategory) { %>
                        <a href="/categories/<%= category.slug %>/trash" class="btn btn-<% if (category.isTrashed) { %>primary<% } else { %>secondary<% } %> btn-sm"><i class="fa-solid fa-eye<% if (!category.isTrashed) { %>-slash<% } %>"></i></a>
                        <a href="/categories/<%= category.slug %>" class="btn btn-info btn-sm mx-2"><i class="fa-solid fa-pen-to-square"></i></a>
                        <a href="/categories/<%= category.slug %>/delete" class="btn btn-danger btn-sm"><i class="fa-solid fa-trash"></i></a>
                        <% } %>
                    </td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</div>
